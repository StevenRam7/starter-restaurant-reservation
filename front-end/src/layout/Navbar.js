import React from "react";
import { Link } from "react-router-dom";

function Navbar() {
  return (
    <nav class="navbar fixed-top navbar-dark bg-dark">
        {/* add fixed navbar to top of page */}
    </nav>
  )
}

export default Navbar;
